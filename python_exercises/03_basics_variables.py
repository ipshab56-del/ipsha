# Exercise 3: Variables Exercise
# Create a variable with an integer value, a variable with a float value, and a
# variable with a string value. Print all three on a single line. Additionally,
# reassign the variable existing_variable to a new value.

int_value = 42
float_value = 3.14
string_value = "Hello"

print(int_value, float_value, string_value)

# Reassign existing variable
existing_variable = 100
existing_variable = 200
print(f"Reassigned variable: {existing_variable}")
